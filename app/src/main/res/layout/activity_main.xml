<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="vertical">

    <!-- Direct child of ScrollView: LinearLayout with wrap_content height -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Replace these sample UI elements with your real ones -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Welcome to the Order App"
            android:textSize="24sp"
            android:layout_marginBottom="16dp"/>

        <EditText
            android:id="@+id/inputName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your name" />

        <!-- Continue adding your Buttons, CheckBoxes, etc. -->
        <!-- For example: -->
        <!-- <CheckBox android:id="@+id/checkboxWhippedCream" ... /> -->
        <!-- <Button android:id="@+id/buttonSubmit" ... /> -->
        <!-- <TextView android:id="@+id/orderSummaryText" ... /> -->

    </LinearLayout>
</ScrollView>